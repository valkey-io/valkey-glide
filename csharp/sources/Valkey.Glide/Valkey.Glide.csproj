<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="$(MSBuildThisFileDirectory)../../props/version.props"/>
  <Import Project="$(MSBuildThisFileDirectory)../../props/common.props"/>

  <PropertyGroup>
    <RootNamespace>Valkey.Glide</RootNamespace>
    <AssemblyName>Valkey.Glide</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- ToDo: Clean up for proper packing
               Right now only the hosts corresponding platform will be included, which is fine for local testing
               (assuming we are using x64. Do not test on x86 or all hell will let loose without runtime-based detection, due to AnyCPU),
               but for the final NuGet, we should include either _all_ assets or _none_
               This could be done by integrating this into the pack target and changing the project references then eg.
               Still need to discuss this, so this package, for now, will default to _none_ -->

    <!-- Windows -->
    <ProjectReference Condition="'$(OS)' == 'Windows_NT'"
                      Include="..\Valkey.Glide.Assets.Win64\Valkey.Glide.Assets.Win64.csproj"/>
  </ItemGroup>

</Project>

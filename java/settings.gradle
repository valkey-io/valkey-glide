// If no matching toolchain is found, this plugin lets Gradle automatically download a matching one.
plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.10.0'
}

rootProject.name = 'glide'

include 'client'
include 'integTest'
include 'benchmarks'

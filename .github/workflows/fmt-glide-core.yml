# Nightly full-matrix tests for the glide-core crate.
# Thin wrapper around glide-core.yml that isolates scheduling so per-language
# full-matrix runs can be staggered and failures do not cascade across
# languages. Flaky-test isolation work: see #4629.
name: FMT glide-core

permissions:
    contents: read
    actions: write

on:
    # Nightly at 05:00 UTC (10:00 PM PDT).
    schedule:
        - cron: "0 5 * * *"
    workflow_dispatch:
        inputs:
            use-self-hosted-linux:
                description: "Use self-hosted runners for Linux x64"
                type: boolean
                default: false

concurrency:
    group: fmt-glide-core-${{ github.ref }}
    cancel-in-progress: true

jobs:
    run-fmt-glide-core:
        if: github.repository_owner == 'valkey-io'
        uses: ./.github/workflows/glide-core.yml
        with:
            run-with-macos: use-self-hosted
            use-self-hosted-linux: ${{ inputs.use-self-hosted-linux || false }}
        name: FMT - glide-core

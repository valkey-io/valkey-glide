[package]
name = "mock-glide-core"
version = "0.0.0"
authors = ["Valkey GLIDE Maintainers"]
license = "Apache-2.0"

# TODO #7175: Specify rust-version
edition = "2024"

[dependencies]

# Mock crates for testing
glide-telemetry = { path = "../mock-glide-telemetry", package = "mock-glide-telemetry" }
redis = { path = "../mock-glide-core-engine", package = "mock-glide-core-engine" }

# GLIDE dependencies
glide-core = { path = "../../../glide-core", features = ["proto"] }

# External dependencies
protobuf = { version = "3", features = [] }
tokio = { version = "^1", features = ["rt", "macros", "rt-multi-thread", "time"] }
